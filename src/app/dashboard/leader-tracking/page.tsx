"use client";

import { LeaderTrackingContent } from "@/components/leader-tracking-content";

// Esta página pode ser mantida como alias ou removida do menu
// Se for mantida, ela apenas renderiza o conteúdo
export default function LeaderTrackingPage() {
  return <LeaderTrackingContent />;
}
